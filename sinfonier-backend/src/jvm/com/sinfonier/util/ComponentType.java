package com.sinfonier.util;

public enum ComponentType {
    /**
     * Define that a component is a bolt
     */
    BOLT,

    /**
     * Define that a component is a spout
     */
    SPOUT,

    /**
     * Define that a component is a drain
     */
    DRAIN,

    /**
     * Define topology options
     */
    OPTIONS
}
